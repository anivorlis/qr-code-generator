[project]
name = "qr-code-generator"
version = "0.1.0"
description = "Turn a URL into a QR code — static frontend + Python serverless backend"
requires-python = ">=3.9"
dependencies = ["segno==1.6.6"]
